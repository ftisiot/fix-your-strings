# Fix your strings talk notebook

This is the notebook for my Fix your strings talk at PG Day Paris, head to the [fix-your-strings.ipynb](fix-your-strings.ipynb) to see the details

## Requirements

It requires

* ``sqlalchemy``
* ``psycopg2`` (or ``psycopg2-binary``)